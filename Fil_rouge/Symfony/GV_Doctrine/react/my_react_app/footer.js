import React from "react";
import ReactDOM from "react-dom/client";
import Footer from "./jsx/Footer.jsx";

ReactDOM.createRoot(document.getElementById("footer-react")).render(
  <React.StrictMode>
    <Footer />
  </React.StrictMode>
);
